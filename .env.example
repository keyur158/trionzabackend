DATABASE_URL="postgresql://trionza_user:changeme@localhost:5432/trionza_diamond"
PORT=3000
JWT_SECRET=change-me-to-a-256-bit-random-string
# Optional: lifetime of newly issued JWTs (defaults to 30d).
JWT_EXPIRES_IN=30d
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
SHOPIFY_CLIENT_ID=xxxxx
SHOPIFY_CLIENT_SECRET=xxxxx
SHOPIFY_API_VERSION=2025-10
SHOPIFY_STOREFRONT_ACCESS_TOKEN=xxxxx
SHOPIFY_WEBHOOK_SECRET=xxxxx
PAYPAL_CLIENT_ID=your-paypal-client-id
PAYPAL_SECRET=your-paypal-secret
PAYPAL_MODE=sandbox
ZEPTOMAIL_TOKEN=
# Region-bound: .com (US, default), .eu (Europe), .in (India). Must match where the token was created.
ZEPTOMAIL_API_BASE=https://api.zeptomail.in
ZEPTOMAIL_FROM_ADDRESS=noreply@trionzadiamond.com
ZEPTOMAIL_FROM_NAME=Trionza Diamond
# Inbox that receives appointment + custom-order notifications.
ADMIN_EMAIL=admin@example.com
# Comma-separated list of admin emails allowed into the admin API/panel.
ADMIN_EMAILS=admin@example.com
APP_PUBLIC_URL=https://trionzadiamond.com
FIREBASE_SERVICE_ACCOUNT=./firebase-service-account.json
SYNC_INTERVAL_HOURS=6
# Opt-in gate for the test checkout endpoint (never enable in production).
ENABLE_TEST_CHECKOUT=false
# Meta (Facebook) Conversions API — leave empty to disable entirely.
# Pixel/Dataset ID from Events Manager; token from Dataset Settings > Conversions API.
META_PIXEL_ID=
META_CAPI_ACCESS_TOKEN=
META_GRAPH_API_VERSION=v23.0
# Test Events code (TEST####) — set ONLY while verifying, remove in production.
META_TEST_EVENT_CODE=